/*97.Write a JavaScript program to find the shortest possible string. This can be converted into a string and converted
into a palindrome by adding characters to the end of it.*/
function shortStr(x) {
    let arr = x.split(" ");
    console.log(arr[0]);
}

let str = "Write a JavaScript program to find the shortest possible string"

shortStr(str);